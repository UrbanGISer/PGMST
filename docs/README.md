# PGMST documentation

- **[PYPI.md](PYPI.md)** — build source/wheel and upload to TestPyPI / PyPI with `twine`.

The user-facing package overview is in the repository **[README.md](../README.md)**.

The **Florida demo** notebook lives in **[../examples/PGMST_Florida_demo.ipynb](../examples/PGMST_Florida_demo.ipynb)** (also vendored into `pgmst/examples/` in the wheel).
